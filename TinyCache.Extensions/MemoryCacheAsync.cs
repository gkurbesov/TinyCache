using System;
using System.Collections.Concurrent;
using System.Collections.Generic;
using System.Text;
using System.Threading;

namespace TinyCache.Extensions
{
    public class MemoryCacheAsync<T> //: IMemoryCache<T> where T : class
    {
        //private readonly MemoryCache<T> cache;
        //private readonly ConcurrentDictionary<object, SemaphoreSlim> locks;

        //public MemoryCacheAsync(MemoryCacheOptions options)
        //{
        //    cache = new MemoryCache<T>(options);
        //    locks = new ConcurrentDictionary<object, SemaphoreSlim>();
        //}

    }
}
